| **Subtopic**                                  | **Focus & Purpose**                                    | **Key Concepts / Details**                                                                       | **One-Line Recall**                                                  |
| --------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- |
| **[[1.4.4 Higher-Order Functions]]**          | Functions that operate on other functions             | functional programming paradigm, function composition, data transformation                       | Higher-order functions take/return functions for functional patterns.|
| **[[Functions as First-Class Objects]]**      | Treat functions as values that can be passed          | functions as objects, function assignment, function parameters                                   | Functions are first-class values in Python.                          |
| **[[map() Function]]**                        | Transform collections with functions                  | map(func, iterable), transformation, lazy evaluation, map vs comprehensions                      | map() applies functions to collection elements.                      |
| **[[filter() Function]]**                     | Select elements matching conditions                   | filter(predicate, iterable), selection logic, lazy evaluation, filter vs comprehensions          | filter() selects elements where predicate is True.                   |
| **[[reduce() Function]]**                     | Aggregate collections to single value                 | reduce(func, iterable), accumulation, functools.reduce, fold operation                           | reduce() combines elements using binary operations.                  |
| **[[Functions Returning Functions]]**         | Create functions that return functions                | closure capture, function factories, parametrized function generation                            | Functions can return other functions with captured state.            |
| **[[Callback Patterns]]**                     | Use functions as callbacks in algorithms               | callback patterns, event handling, continuation patterns, inversion of control                    | Callbacks enable event-driven and asynchronous patterns.             |
| **[[Function Composition]]**                  | Combine functions into larger computations            | composition patterns, chaining operations, pipeline creation, reduce with composition            | Composition chains functions for complex transformations.            |
