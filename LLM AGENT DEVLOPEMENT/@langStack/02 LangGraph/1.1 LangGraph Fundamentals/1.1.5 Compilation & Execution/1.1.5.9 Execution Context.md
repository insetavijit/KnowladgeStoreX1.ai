Below is a **similar-styled, interview-focused table** for **[[1.1.5.9 Execution Context]]**, covering runtime configuration.

|**Section**|**Interview Learning Goals**|**Key Technical Details to Study**|**One-Line Recall**|
|---|---|---|---|
|**Execution Context Overview**|Explain the configuration and metadata that defines an execution session.**Execution context; configuration; metadata; runtime setup; context passing; configuration options; execution environment.**Execution context provides configuration and metadata for a specific graph execution run.|
|**[[config Parameter in invoke()]]**|Describe passing configuration when invoking a graph.**config dict; invoke(config=...); configuration passing; context setup; optional parameter; runtime configuration.**Pass optional config parameter to invoke() to specify execution context and configuration.|
|**[[config Parameter in stream()]]**|Explain configuration for streaming execution.**config dict; stream(config=...); configuration; context setup; streaming configuration; optional parameter.**Pass optional config parameter to stream() similarly; configuration applies to streaming execution.|
|**[[Thread ID in Execution Context]]**|Describe the thread_id that scopes checkpoint storage.**thread_id; execution scoping; checkpoint namespace; thread separation; session identification; context element; scope management.**thread_id in config distinguishes different execution threads; each thread has separate checkpoint namespace.|
|**[[Checkpoint ID in Config]]**|Explain checkpoint_id for identifying specific saved states.**checkpoint_id; state versioning; session tracking; multiple checkpoints; checkpoint naming; identification; state versions.**checkpoint_id in config identifies which checkpoint to load for resumption; enables multi-session management.|
|**[[Input State Structure]]**|Describe the input dict structure for initializing state.**Initial state dict; input format; field structure; type matching; schema compliance; input structure; state initialization.**Input dict to invoke()/stream() must match state schema; provides initial values for state fields.|
|**[[Required vs Optional Context Fields]]**|Explain which context fields are required vs optional.**Required fields; optional fields; defaults; optional parameters; minimal config; configuration flexibility.**thread_id typically required; checkpoint_id optional; other fields optional; sensible defaults if omitted.|
|**[[Context Metadata]]**|Describe additional metadata passed in execution context.**Metadata; custom metadata; tracing; correlation IDs; request IDs; context enrichment; additional context; metadata passing.**config can include custom metadata: tracing IDs, correlation IDs, user info; accessible in nodes via state.|
|**[[Configuration Access in Nodes]]**|Show how nodes can access execution configuration.**Access patterns; reading config; context access; metadata access; node-level access; configuration retrieval; accessing context.**Nodes can access config via closure or special parameters (depends on framework implementation).**
|**[[Per-Invocation Configuration]]**|Describe configuration passed per execution, not per-compilation.**Compile-time config; runtime config; per-execution setup; flexibility; invocation-specific; customization; dynamic configuration.**config is per-invocation; doesn't require recompilation; enables dynamic configuration per run.|
|**[[Session/User Context]]**|Show passing user/session information via context.**User context; session ID; user ID; session tracking; multi-tenant; user identification; context enrichment; multi-user support.**Pass user ID, session ID in config; enables multi-user/multi-tenant support; tracks sessions.**
|**[[Checkpointer Configuration]]**|Describe configuring checkpointer behavior per execution.**Checkpointer settings; persistence configuration; per-execution setup; configuration passing; runtime options; checkpoint control.**Checkpointer configuration can be passed per execution; enables flexible persistence per run.|
|**[[Timeout Configuration]]**|Explain setting execution timeouts in context.**Timeout setting; time limits; bounded execution; execution timeout; configuration; safety; runaway prevention; time management.**Configuration can include execution timeout; framework enforces time limits on execution; prevents hanging.**
|**[[Custom Context Data]]**|Show passing custom application data via context.**Custom fields; app-specific data; context enrichment; data passing; arbitrary data; flexibility; extension; custom information.**config dict can include custom fields for application-specific needs; accessible in execution context.|
|**[[Context Propagation to Nodes]]**|Describe how context flows to node functions.**Context availability; node access; scope; propagation; availability; passing data; context reach; information flow.**Context available throughout execution; nodes can access via framework hooks or state enrichment.**
|**[[Context Immutability]]**|Explain that context shouldn't be modified during execution.**Immutability; read-only access; no mutations; consistency; determinism; safety; side-effect avoidance; purity principle.**Context should be treated as read-only; modifying context during execution can cause issues.|

If you want, I can next create similar tables for **[[1.1.5.10 Async Execution]]** or **[[1.1.5.11 Execution Performance]]** to continue this context track.
