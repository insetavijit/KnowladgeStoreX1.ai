| **Subtopic**                            | **Focus & Purpose**                                            | **Key Concepts / Details**                                                                 | **One-Line Recall**                                                    |
| --------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| **[[4.1.7 Encoding & Unicode Overview]]**| Represent text binaries correctly                              | bytes vs strings, universal character sets, legacy encodings, decoding strategies          | Encoding is the bridge between human characters and machine bits.      |
| **[[Character Encoding]]**              | Map characters to integer code points                          | ASCII (7-bit), ISO-8859-1 (Latin-1), Windows-1252, code pages, variable-width encoding     | Computers don't "see" letters; they only see numeric codes.            |
| **[[UTF-8]]**                           | The dominant universal encoding standard                       | 1-4 bytes per char, backward compatible with ASCII, self-synchronizing, default for web    | UTF-8 is the "one encoding to rule them all" for modern NLP.           |
| **[[Unicode Handling]]**                | Work with the full range of human characters                   | code points (`U+1F600`), glyphs, grapheme clusters, surrogate pairs, plane 0 (BMP)         | Unicode assigns a unique ID to every character, no matter the platform.|
| **[[Encoding Errors]]**                 | Handle mismatched encoding/decoding attempts                   | `UnicodeDecodeError`, `UnicodeEncodeError`, `replace` vs `ignore` handlers, mojibake       | "Ã©" instead of "é" is the classic sign of an encoding mismatch.       |
| **[[Multilingual Text]]**               | process diverse scripts within a single stream                 | script detection, RTL (right-to-left) support, language-specific rendering, font fallback  | True NLP mastery means handling text that mixes English, Arabic, and Emoji.|
