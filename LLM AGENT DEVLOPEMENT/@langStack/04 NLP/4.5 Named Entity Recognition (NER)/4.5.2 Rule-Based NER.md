| **Subtopic**                            | **Focus & Purpose**                                            | **Key Concepts / Details**                                                                 | **One-Line Recall**                                                    |
| --------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| **[[4.5.2 Rule-Based NER Overview]]**   | Extract entities using deterministic patterns                  | precision-first approach, zero-training, domain-specific logic, regex power                | When you know exactly what you are looking for, don't guess—match it.  |
| **[[Regular Expressions]]**             | Pattern matching for structured formats                        | emails, phone numbers, dates, SKU codes, simple capitalization rules                       | Regex is the king of extracting "DD-MM-YYYY".                          |
| **[[Gazetteers]]**                      | Dictionary-based lookups                                       | lists of cities, lists of medications, huge hashmaps, trie-based matching (Aho-Corasick)   | If you have a list of every city on Earth, just check the list.        |
| **[[SpaCy EntityRuler]]**               | Combining rules with statistical models                        | `.add_pipe("entity_ruler")`, patterns `[{"LOWER": "apple"}, {"LOWER": "pay"}]`            | Injecting manual rules into a machine learning pipeline.               |
| **[[Deterministic Extraction]]**        | Guaranteed behavior without probability                        | high precision, low recall (brittleness), easy to debug, no training data needed           | Rules don't hallucinate.                                               |
| **[[FlashText]]**                       | Efficient keyword replacement/extraction                       | alternative to regex for thousands of keywords, standard algorithm for massive list matching| Searching for 100,000 different keywords in one pass.                  |
