
| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **[[2.4.5 Agent Executors]]** | The runtime environment | managing the agent loop, handling errors, timeout logic | The engine that runs the agent's thought loop. |
| **[[AgentExecutor Class]]** | The main runner | `AgentExecutor(agent=..., tools=...)`, legacy chain wrapper | The standard class to execute an agent. |
| **[[Max Iterations]]** | Preventing infinite loops | `max_iterations=5`, stopping runaways | A safety brake to stop the agent from thinking forever. |
| **[[Timeouts]]** | Execution limits | `max_execution_time`, aborting long steps | Forcing the agent to give up if it takes too long. |
| **[[Early Stopping]]** | Determining "Done" | logic for when to return the final answer | Deciding when the task is actually finished. |
| **[[Parsing Errors]]** | Handling confusion | `handle_parsing_errors=True`, auto-correcting malformed output | Fixing the agent when it outputs bad JSON or XML. |
| **[[Intermediate Steps]]** | Transparency | `return_intermediate_steps=True`, showing the work | Seeing the "Though" and "Observation" log in the final result. |
