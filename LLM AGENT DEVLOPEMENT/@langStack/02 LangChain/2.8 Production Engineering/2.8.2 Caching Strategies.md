
| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **[[2.8.2 Caching Strategies]]** | Performance | storing previous results to save money and time | "Don't think twice if you've already answered this." |
| **[[InMemoryCache]]** | RAM storage | fast, ephemeral cache for development or single sessions | Fast but disappears when the script stops. |
| **[[Redis Cache]]** | Shared storage | using Redis to share cache across many production servers | The standard way to cache in production. |
| **[[SQLite Cache]]** | Disk storage | easy file-based persistence for single-server apps | Simple, persistent caching without a dedicated server. |
| **[[Cache Keys]]** | Identification | hashing prompt + parameters to create a unique lookup key | How we look up if we've seen this question before. |
| **[[Cache Invalidation]]** | Freshness | setting TTL (Time To Live) so old answers don't persist forever | Deleting old answers so they don't get stale. |
| **[[Semantic Cache]]** | Smart lookup | using embeddings to return cached answers for *similar* questions | Returning the same answer for "Hello" and "Hi". |
