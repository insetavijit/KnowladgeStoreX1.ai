
| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **[[2.10.7 Configuration & Context]]** | Runtime Control | changing chain behavior at runtime without changing code | Adjusting the settings knobs while the engine is running. |
| **[[RunnableConfig]]** | The Object | the dictionary passed to `invoke(..., config={...})` | The standard way to pass "extra stuff" to a chain. |
| **[[Configurable Chains]]** | Adaptability | `configurable_fields` and `configurable_alternatives` | "Let the user choose GPT-3 or GPT-4 at runtime." |
| **[[Runtime Context]]** | metadata | passing user IDs, session IDs, or tags in the config | "This request belongs to User 123." |
| **[[Environment]]** | Globals | accessing global settings or secrets within a runnable | Reading the room. |
| **[[Tags]]** | Organization | tagging runs for easier filtering in LangSmith | "Label this run as 'experiment-A'." |
| **[[Chain Configuration]]** | Recursion | how config propagates down to all children in a nested chain | "If I set the timeout at the top, it applies to everything." |
