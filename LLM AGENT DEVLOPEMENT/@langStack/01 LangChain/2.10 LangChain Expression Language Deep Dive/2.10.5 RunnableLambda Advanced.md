
| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **[[2.10.5 RunnableLambda Advanced]]** | Custom Logic | turning arbitrary Python functions into chainable steps | Wrapping your messy code in a neat box. |
| **[[Custom Functions]]** | Integration | `RunnableLambda(my_func)`, executing standard Python code within LCEL | "Run this random python script as step 3." |
| **[[Async Lambdas]]** | Concurrency | using `async def` functions for non-blocking execution | "Don't block the thread while you wait for the database." |
| **[[Error Handling]]** | Resilience | wrapping functions to handle exceptions gracefully within the stream | Catching bugs inside the custom function. |
| **[[Logging]]** | Observability | adding print statements or logging calls inside the lambda | "Print 'Hello' every time this runs." |
| **[[Side Effects]]** | Actions | functions that do things (send email) rather than return data | "Send an email to the user." |
| **[[Lambda Patterns]]** | Strategy | using lambdas for small transformations (e.g., `lambda x: x['text']`) | Quick one-liners to clean up data. |
