
| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **[[2.1.6 Output Parsers]]** | Structuring LLM responses | converting raw text into usable data structures (JSON, Lists) | Transforms unstructured LLM text into structured data. |
| **[[StrOutputParser]]** | Simple text extraction | removing artifacts, basic string cleanup, standardizing text | The simplest parser that just returns the string content. |
| **[[JsonOutputParser]]** | Guaranteed JSON handling | strict JSON formatting, repairing malformed JSON, schema enforcement | Ensures the LLM output is valid, parseable JSON. |
| **[[PydanticOutputParser]]** | Strict schema validation | validating output against Python classes, data typing, automatic checks | Parses output directly into a validated Pydantic object. |
| **[[Structured Output]]** | Defining expected formats | defining schemas, format instructions, few-shot formatting examples | Guiding the LLM to generate specific data structures. |
| **[[Error Handling]]** | Managing parsing failures | `RetryOutputParser`, auto-fixing parsers, fallback logic | Strategies to recover when the LLM outputs invalid formats. |
| **[[Custom Parsers]]** | Domain-specific logic | extending `BaseOutputParser`, custom parsing rules, legacy parsing | Write your own parser for unique data requirements. |
