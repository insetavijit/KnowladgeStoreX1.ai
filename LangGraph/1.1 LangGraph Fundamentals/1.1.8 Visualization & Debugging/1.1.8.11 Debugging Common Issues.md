Below is a **similar-styled, interview-focused table** for **[[1.1.8.11 Debugging Common Issues]]**, covering troubleshooting frequent problems.

|**Section**|**Interview Learning Goals**|**Key Technical Details to Study**|**One-Line Recall**|
|---|---|---|---|
|**Debugging Common Issues Overview**|Explain typical problems and investigation approach.**Common issues; typical problems; problem categories; debugging approach; systematic investigation; problem diagnosis; solution approach; common scenarios; frequent issues; problem taxonomy; troubleshooting guide; diagnosis methodology; solution strategy.**Common issues in LangGraph: infinite loops, state corruption, routing errors, node failures, integration failures, performance issues; recognize symptoms, diagnosis approach, systematic solution; troubleshooting framework; problem recognition; solution pathway; debugging strategy.**
|**[[Infinite Loop Detection]]**|Describe identifying and fixing infinite loops.**Infinite loops; runaway execution; non-terminating; hanging; stuck execution; loop termination failure; exit condition failure; cycle without exit; detection; diagnosis; prevention; fix approach; loop safety; termination guarantee.**Detect infinite loops: execution never completes, CPU spinning, timeout, excessive iterations logged; probable causes: exit condition never met, wrong condition, missing break condition; fix: add iteration limit, verify condition logic, add safety timeout; prevention: always have iteration limit AND exit condition; safety assurance.**
|**[[State Corruption Issues]]**|Explain diagnosing state problems.**State corruption; wrong state; unexpected state; state bug; state error; state inconsistency; invalid state; state mismatch; diagnosis; identifying cause; fixing state; state verification; state validation; state integrity; state correctness; state assurance.**State corruption symptoms: state field has wrong value, missing fields, unexpected structure; probable causes: node returns wrong value, state schema mismatch, side effects, race condition (async); debug: log state at each node, compare to expected, trace mutations; fix: verify node return values, check side effects, validate schema; prevention: type validation, testing.**
|**[[Routing Errors]]**|Describe routing failures and fixes.**Routing errors; wrong routing; unexpected path; wrong node; routing failure; incorrect branch; decision error; routing problem; diagnosis; routing verification; routing correction; routing confidence; routing correctness; routing assurance; routing validation; routing success.**Routing error symptoms: wrong node executes, unexpected path taken, correct condition but wrong routing; probable causes: routing function bug, wrong condition name, typo in node name; debug: log routing decisions, verify condition values, check routing function logic; fix: correct routing function, verify node names, test all conditions; prevention: type-safe node names, routing testing.**
|**[[Node Failures]]**|Explain handling node execution errors.**Node failures; exceptions; errors in nodes; node crash; execution failure; error propagation; exception handling; node reliability; error diagnosis; error investigation; error handling; error recovery; error prevention; fault tolerance; reliability; robustness.**Node failure symptoms: exception in execution logs, error stack trace, stopped execution; probable causes: bad input, unhandled exception, missing dependency, API failure; debug: read error message, check stack trace, log input, verify dependencies; fix: add error handling, validate inputs, check dependencies, handle edge cases; prevention: robust error handling, input validation, dependency checks.**
|**[[Integration Failures]]**|Describe diagnosing integration problems.**Integration failures; component failures; tool failures; API failures; dependency failures; missing integration; connection failures; compatibility issues; integration error; diagnosis; fixing integration; integration verification; integration testing; integration confidence; integration assurance; integration reliability.**Integration failure symptoms: external tool fails, API call fails, data format mismatch, connection error; probable causes: API down, credential issue, data format incompatibility, timeout, rate limit; debug: check API status, verify credentials, log request/response, check format; fix: retry logic, error handling, format validation, timeouts, rate limiting; prevention: robust integration, error handling, monitoring.**
|**[[Performance Degradation]]**|Explain investigating slow execution.**Performance degradation; slow execution; latency increase; throughput decrease; degradation; slowdown; performance problem; diagnosis; optimization; bottleneck identification; slow components; efficiency loss; performance issue; performance problem diagnosis; optimization target; improvement opportunity.**Performance degradation symptoms: slower than expected, increasing latency, decreased throughput, timeouts; probable causes: slow node added, resource contention, network latency, data growth; debug: profile execution, identify slow nodes, measure latencies, check resource usage; fix: optimize slow nodes, add caching, parallelize, reduce data; prevention: baseline performance, monitor trends, proactive optimization.**
|**[[Memory Issues]]**|Describe identifying memory problems.**Memory issues; memory leak; memory growth; memory consumption; excessive memory; memory pressure; memory problem; diagnosis; memory investigation; memory optimization; memory efficiency; memory management; memory awareness; memory monitoring; resource management; resource optimization.**Memory issue symptoms: increasing memory consumption, OOM error, memory leak, process becomes slow; probable causes: state accumulation, not cleaning up, reference cycles, large state; debug: profile memory, find memory leak source, check state size, trace allocations; fix: clean up state, limit state size, manage references, release resources; prevention: memory monitoring, state discipline, periodic cleanup, size monitoring.**
|**[[State Schema Mismatches]]**|Explain schema-related issues.**Schema mismatches; type mismatches; field mismatches; schema validation; type validation; incompatibility; schema error; incompatibility; diagnosis; fixing schema; validation; schema verification; type safety; schema correctness; type correctness; schema assurance; type assurance.**Schema mismatch symptoms: type error, field missing, field type wrong, schema validation fails; probable causes: node returns wrong type, schema change not updated, field removed but used; debug: check error message, verify return types, read schema definition; fix: correct node return type, update schema, fix field references; prevention: type checking (mypy), schema validation, testing.**
|**[[Race Conditions (Async Issues)]]**|Describe async/concurrency problems.**Race conditions; concurrency issues; ordering issues; data races; timing issues; ordering bugs; non-deterministic behavior; non-determinism; ordering problem; synchronization failure; diagnosis; fixing race; async debugging; concurrency safety; correctness; reliability; determinism; predictability.**Race condition symptoms: non-deterministic behavior, occasional failures, timing-dependent, intermittent errors; probable causes: missing synchronization, concurrent mutation, ordering assumptions; debug: add logging, increase contention (make issue more likely), use async debuggers, check synchronization; fix: add synchronization, ensure atomic operations, correct ordering; prevention: synchronization discipline, immutability, testing under load.**
|**[[Timeout Issues]]**|Describe handling timeout problems.**Timeouts; timeout errors; slow responses; unresponsive; stuck execution; delayed execution; timeout problem; diagnosis; timeout configuration; timeout handling; timeout management; timeout value; timeout strategy; timeout resilience; reliability; responsiveness.**Timeout symptoms: execution doesn't complete in time, timeout error, stuck response, waiting; probable causes: slow node, external service slow, network delay, hanging; debug: measure node latencies, identify slow component, check external service; fix: optimize slow node, increase timeout value, add timeout handling, fallback; prevention: reasonable timeouts, performance monitoring, SLA awareness.**
|**[[Error Message Interpretation]]**|Describe understanding error messages.**Error messages; interpreting errors; error diagnosis; reading errors; stack traces; understanding errors; error clarity; error information; debugging aid; error understanding; problem identification; error description; diagnostic information; error context; error guidance; helpful errors; informative errors.**Learn to read error messages: message describes problem, stack trace shows location, context shows what was executing; understand error context, trace back to cause, identify problem systematically; diagnostic information; error interpretation skill; problem identification; location finding; cause discovery; systematic diagnosis; evidence-based investigation.**
|**[[Configuration Issues]]**|Explain diagnosing setup problems.**Configuration issues; setup problems; configuration error; initialization failure; setup failure; initialization issue; configuration mistake; wrong config; diagnosis; fixing config; configuration verification; setup correctness; configuration assurance; setup success; initialization success; proper configuration.**Configuration issue symptoms: initialization fails, wrong behavior due to settings, unexpected values; probable causes: wrong API key, wrong path, missing setting, environment variable not set; debug: check configuration, verify environment variables, read documentation; fix: correct configuration, set environment variables, follow setup guide; prevention: configuration validation, documentation, checklists.**
|**[[Testing Issues for Debugging]]**|Describe using tests for problem diagnosis.**Testing; unit testing; test failures; test-driven debugging; test failures diagnosis; failed tests; test-based diagnosis; testing; using tests; reproducibility; test failures; test debugging; test effectiveness; diagnostic power; reproducibility; systematic testing; problem isolation; focused investigation.**Use tests: reproduce issue in test (narrow problem), isolate component, understand failure, fix and verify; test-driven debugging; reproducibility; systematic investigation; component isolation; test as documentation; test as verification; test effectiveness; problem isolation; systematic approach; focused debugging; confidence building.**
|**[[Systematic Debugging Approach]]**|Describe methodical problem-solving.**Systematic approach; methodology; step-by-step; methodical; observation; hypothesis; testing; iteration; debugging process; problem solving; investigation; evidence; scientific approach; systematic method; proven approach; effective methodology; disciplined debugging; organized approach; structured process.**Systematic approach: (1) observe symptoms, (2) form hypothesis of cause, (3) design test of hypothesis, (4) run test, (5) analyze result, (6) adjust hypothesis and iterate until solved; scientific method; evidence-based; disciplined; systematic; methodical; organized; effective; proven; reproducible; learnable; teachable; skill-building.**

Finally, we'll cover **[[1.1.8.12 Debugging Best Practices & Pitfalls]]** to conclude.
