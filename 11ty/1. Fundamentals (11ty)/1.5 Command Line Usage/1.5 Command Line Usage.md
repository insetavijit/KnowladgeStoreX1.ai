|Topic|Focus & Purpose|
|---|---|
|**[[1.5.1 Basic Commands]]**|`npx eleventy`; running a local build; default behavior; checking version (`--version`) and help (`--help`). CLI basics.|
|**[[1.5.2 Serving & Watching]]**|`--serve` (local server + browsersync); `--watch` (rebuild on change without server); configuring the local server port. Development workflow.|
|**[[1.5.3 Debugging & Dry Runs]]**|`--dryrun` (simulate build without writing files); `DEBUG=Eleventy*` (verbose logging); troubleshooting silent failures. Diagnostics.|
|**[[1.5.4 Advanced Flags]]**|`--input`, `--output`, `--config`; overriding config file settings from the CLI; `--formats` to filter processing used. Runtime overrides.|
|**[[1.5.5 Incremental Builds]]**|`--incremental`; speeding up large site builds; how 11ty determines what to rebuild. Performance tuning.|
