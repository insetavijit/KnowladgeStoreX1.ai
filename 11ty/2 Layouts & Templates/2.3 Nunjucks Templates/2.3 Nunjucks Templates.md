|Topic|Focus & Purpose|
|---|---|
|**[[2.3.1 Variables & Types]]**|Declaring variables; `set` tag; strings, numbers, arrays, objects; scoping rules. Data structures.|
|**[[2.3.2 Filters & Pipe Syntax]]**|Using filters `|`; chaining filters; defining custom filters in config; built-in Nunjucks filters. Data transformation.|
|**[[2.3.3 Control Structures]]**|`if`/`else`/`endif`; `for` loops; loop variables (`loop.index`, `loop.first`); async control. Logic flow.|
|**[[2.3.4 Macros (Reusable Functions)]]**|Defining macros; macro arguments; valid use cases; importing macros. Component logic.|
|**[[2.3.5 Includes & Imports]]**|`include` vs `import` vs `from`; managing dependencies; namespacing in Nunjucks. Modular code.|
