| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **2.3.3 Control Structures** | Logic Flow | `if`, `for`, `else`, `elif`, `async` | Directing the template rendering. |
| **[[2.3.3.1 Conditional Logic]]** | Branching | `{% if condition %}`; `{% elif %}`; `{% else %}`; `{% endif %}` | If this, then that. |
| **[[2.3.3.2 Loops (For In)]]** | Iteration | `{% for item in array %}`; `Dict` iteration; usage of `loop.index` | Repeat for every item. |
| **[[2.3.3.3 Loop Variables]]** | Context | `loop.index`, `loop.first`, `loop.last`, `loop.length` | Knowing where you are in the loop. |
| **[[2.3.3.4 Break & Continue]]** | Flow Control | Exiting loops early; skipping iterations | Stop or skip. |
| **[[2.3.3.5 Async Control]]** | Advanced | `{% asyncAll %}`; `{% asyncEach %}` (Nunjucks specific extensions) | Handling async data in loops. |
