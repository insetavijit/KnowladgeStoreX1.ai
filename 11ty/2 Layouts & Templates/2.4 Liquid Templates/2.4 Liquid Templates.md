|Topic|Focus & Purpose|
|---|---|
|**[[2.4.1 Objects & Tags]]**|Output markup `{{ }}`; Tag markup `{% %}`; understanding Liquid's syntax constraints. Core syntax.|
|**[[2.4.2 Standard Filters]]**|Common filters: `date`, `capitalize`, `append`, `split`; mapping arrays. String & array manipulation.|
|**[[2.4.3 Iteration & Logic]]**|`for` loops; `tablerow`; `if`/`unless`/`elsif`; `case`/`when`. Flow control.|
|**[[2.4.4 Assignments & Capture]]**|`assign` variables; `capture` blocks for complex string building. Variable management.|
|**[[2.4.5 Jekyll Compatibility]]**|Migrating from Jekyll; quirks and differences; utilizing existing Liquid knowledge. Migration path.|
