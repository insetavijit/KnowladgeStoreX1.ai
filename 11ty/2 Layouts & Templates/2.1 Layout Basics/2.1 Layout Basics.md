|Topic|Focus & Purpose|
|---|---|
|**[[2.1.1 Creating Layouts]]**|Understanding the `_includes` directory; creating a basic layout file; wrapper templates; `{{ content }}` variable. Structure foundation.|
|**[[2.1.2 Layout Aliasing]]**|Mapping layout aliases in config; simplifying layout usage; refactoring layout names without breaking content. Configuration convenience.|
|**[[2.1.3 Directory Specific Layouts]]**|Setting layouts via directory data files (`directory.json`); applying layouts to entire folders. Scoped application.|
|**[[2.1.4 Layout Chaining (Parent/Child)]]**|Nesting layouts; a layout using another layout; building a layout hierarchy (e.g., `base` -> `page` -> `post`). Modular design.|
|**[[2.1.5 Front Matter Layout Assignment]]**|Specifying `layout: name` in front matter; overriding directory defaults; bypassing layouts. Direct control.|
