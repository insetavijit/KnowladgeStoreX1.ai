| **Subtopic** | **Focus & Purpose** | **Key Concepts / Details** | **One-Line Recall** |
| :--- | :--- | :--- | :--- |
| **2.1.1 Creating Layouts** | Foundation of templating | `_includes`, `base.njk`, `{{ content }}` | The wrapper for your content. |
| **[[2.1.1.1 The _includes Directory]]** | Storage location | Default path; where layouts live; preventing direct output | Layouts live in `_includes`. |
| **[[2.1.1.2 Base Layout Structure]]** | HTML Boilerplate | `<html>`, `<head>`, `<body>`; the root template; DOCTYPE | The shell of your website. |
| **[[2.1.1.3 The Content Variable]]** | Injection point | `{{ content }}` (Liquid) / `{{ content \| safe }}` (Nunjucks); where page body goes | `content` is where the page body lands. |
| **[[2.1.1.4 Creating a Wrapper]]** | Basic implementation | Creating `_includes/base.njk`; connecting front matter | Wrap markdown with HTML. |
| **[[2.1.1.5 Layout Resolution]]** | Finding the file | How `layout: base` maps to `_includes/base.njk`; extension usage | 11ty knows where to look. |
