**Overview**  
A WordPress theme is recognized and loaded based on a small set of mandatory files and headers. The presence of a properly formatted `style.css` header allows WordPress to identify the theme, 
while `index.php` serves as the ultimate fallback template in the rendering hierarchy. Together with an optional but standard `functions.php` and an admin-facing `screenshot.png`, these files form the **minimum viable theme**. Understanding these requirements is essential because WordPress will not register, activate, or render a theme unless this baseline structure is met, regardless of how advanced the theme’s features may be.

| **Section**                            | **Interview Learning Goals**                                             | **Key Technical Details to Study**                                                              |
| -------------------------------------- | ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| **Structure Overview**                 | Explain what makes a WordPress theme valid and how WordPress detects it. | Theme directory in `/wp-content/themes/`; required files; role of headers in discovery.         |
| **[[style.css Headers]]**              | Describe how theme metadata is declared.                                 | `Theme Name`, `Author`, `Version`, `Text Domain`, `Requires at least`, `Requires PHP`.          |
| **[[index.php]]**                      | Explain the role of the fallback template.                               | Main template file; required even if unused; last resort in template hierarchy.                 |
| **[[functions.php]]**                  | Explain how theme functionality is added.                                | Enqueue scripts/styles; theme supports; hooks; no closing PHP tag best practice.                |
| **[[screenshot.png]]**                 | Explain how the theme is visually identified in admin.                   | 1200×900px recommended; PNG/JPG; displayed in Appearance → Themes.                              |
| **[[Theme Folder Naming]]**            | Discuss best practices for theme directory names.                        | Lowercase; no spaces; unique slug; matches text domain.                                         |
| **[[Child Theme Requirements]]**       | Explain what extra files are needed for a child theme.                   | `style.css` with `Template:` header; optional `functions.php`; inherits parent templates.       |
| **[[Theme Activation Checks]]**        | Describe how WordPress validates a theme.                                | Missing file warnings; fatal error handling; admin notices; fallback to default theme.          |
| **[[Classic vs Block Minimum Files]]** | Compare minimum files for classic vs block themes.                       | Classic: `style.css` + `index.php`; Block: `style.css` + `templates/index.html` + `theme.json`. |
| **[[Common Errors & Pitfalls]]**       | Identify mistakes that break theme recognition.                          | Missing headers; wrong comment syntax; UTF-8 BOM; incorrect file names; permissions.            |
| **[[Theme Validation Tools]]**         | Explain how to verify theme compliance.                                  | Theme Check plugin; WP_DEBUG; manual inspection; coding standards.                              |

If you want, I can generate a matching table for the next topic in **1.3.1** to keep the structure consistent across your syllabus.