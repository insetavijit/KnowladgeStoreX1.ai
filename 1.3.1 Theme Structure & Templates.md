Below is the matching topics table for **[[1.3.1 Theme Structure & Templates]]**, following the same syllabus style and depth:

|**Section**|**Focus**|**Key Utilities / Concepts**|**One-Line Recall**|
|---|---|---|---|
|**[[1.3.1.1 Theme File Requirements]]**|Minimum viable theme|`style.css` headers, `index.php`, `functions.php`, screenshot|A WordPress theme is recognized by required files and headers.|
|**[[1.3.1.2 Theme Folder Structure]]**|Organizing theme files|assets folders, includes, template parts, naming conventions|Clean structure keeps themes readable and maintainable.|
|**[[1.3.1.3 Template Hierarchy]]**|File selection logic|hierarchy order, fallbacks, custom templates|WordPress selects templates based on a defined hierarchy.|
|**[[1.3.1.4 Core Template Files]]**|Standard page types|`index.php`, `single.php`, `page.php`, `archive.php`, `search.php`, `404.php`|Core templates map common request types to PHP files.|
|**[[1.3.1.5 Template Parts & Includes]]**|Reusable layout sections|`get_header()`, `get_footer()`, `get_sidebar()`, `get_template_part()`|Template parts promote reuse and consistent layouts.|
|**[[1.3.1.6 The Loop Fundamentals]]**|Rendering queried content|`have_posts()`, `the_post()`, post globals|The Loop iterates over queried posts to output content.|
|**[[1.3.1.7 WP_Query & Custom Queries]]**|Data retrieval|main query vs secondary, args, pagination, `pre_get_posts`|`WP_Query` fetches content beyond the main query.|
|**[[1.3.1.8 Template Tags]]**|Output helpers|`the_title()`, `the_content()`, `get_permalink()`, conditional tags|Template tags output sanitized WordPress data in themes.|
|**[[1.3.1.9 Conditional Logic in Templates]]**|Context-aware rendering|`is_single()`, `is_page()`, `is_home()`, `is_archive()`|Conditionals adapt templates based on request context.|
|**[[1.3.1.10 Pagination & Navigation]]**|Browsing results|`the_posts_pagination()`, `paginate_links()`, prev/next links|Pagination improves navigation across long lists of posts.|
|**[[1.3.1.11 Escaping & Output Safety]]**|Secure rendering|`esc_html()`, `esc_url()`, `wp_kses()`|Proper escaping keeps theme output secure.|
|**[[1.3.1.12 Testing Hierarchy & Templates]]**|Verification|debug tools, template hints, edge cases|Testing ensures correct templates load in all scenarios.|

If you want, I can continue with **[[1.3.2 Theme Functions & Features]]** to cover `functions.php`, theme supports, menus, widgets, and asset enqueueing.